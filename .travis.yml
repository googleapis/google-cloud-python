language: python
# command to install dependencies
install:
  - pip install tox
  - pip install coveralls
# run tests
script:
  - tox -e py26
  - tox -e py27
  - tox -e lint
  - scripts/run_regression.sh
after_success:
  - tox -e cover
  - coveralls
  - scripts/update_docs.sh
